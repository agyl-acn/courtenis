# Runtime-only dependencies for the AWS Lambda deployment package.
# Excludes uvicorn — on Lambda, Mangum (in lambda_handler.py) is the ASGI
# adapter, so no ASGI server is needed at runtime.
#
# Local development still uses requirements.txt (which includes uvicorn).

# Core
openai-agents>=0.0.7
fastapi>=0.115.0

# Gemini support via LiteLLM
litellm>=1.40.0

# AWS Lambda ASGI adapter
mangum>=0.19.0

# DynamoDB storage backend
boto3>=1.34.0

# Data validation
pydantic>=2.0.0
pydantic-settings>=2.0.0
